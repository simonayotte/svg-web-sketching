//import { CanvasGridDirective } from './canvas-grid.directive';

describe('CanvasGridDirective', () => {
    it('should create an instance', () => {
        //const directive = new CanvasGridDirective();
        //expect(directive).toBeTruthy();
    });
});
